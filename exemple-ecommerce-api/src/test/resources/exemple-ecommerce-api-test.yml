api:
   resourceId: exemple
   authorization:
      path: localhost
      hazelcast:
         port: ${hazelcast.port}
         addresses: 127.0.0.1:${hazelcast.port}
      client:
         clientId: resource
         clientSecret: secret